# The main file

# import
import sys
from cryptography.fernet import Fernet

# import modules
from funct import main_page, get_fernet, add_passwd, get_passwd, ch_pswd, del_pswd

while True:
    change = main_page()
    if change.lower() == 'a':
        f = get_fernet()
        add_passwd(f)
    elif change.lower() == 'g':
        get_passwd()
    elif change.lower() == "c":
        f = get_fernet()
        ch_pswd(f)
    elif change.lower() == 'd':
        del_pswd()
    elif change.lower() == 'q':
        sys.exit(0)
    else:
        continue
